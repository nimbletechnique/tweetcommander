#tweets
  = render :partial => "tweet", :collection => @tweets if @tweets

- if current_user && current_user.can_fetch_more_tweets?
  %script{ :type => "text/javascript" }
    = remote_function :method => :get, :before => "$('spinner').show()", :complete => "$('spinner').hide()"
